language: go

env:
  GO111MODULE=on

go:
  - 1.9.x
  - 1.10.x
  - 1.12.x

go_import_path: github.com/sbinet/go-python

before_install:
   - sudo apt-get update -qq
   - sudo apt-get install python-dev -qq

notifications:
  email:
    recipients:
      - binet@cern.ch
    on_success: change
    on_failure: always
